<form method="post" action="{{ path('app_plannig_delete', {'id': plannig.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plannig.id) }}">
    <button class="btn">Delete</button>
</form>
