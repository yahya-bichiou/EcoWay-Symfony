{% extends 'base.html.twig' %}

{% block title %}Catégorie{% endblock %}

{% block body %}
<div class="card shadow-lg p-4 border border-primary rounded col-12 col-md-8 mx-auto">
    <h2 class="text-center text-primary">{{ categorie.nom }}</h2>
    <h4>Description :</h4>
    <p>{{ categorie.description ?: 'Aucune description disponible.' }}</p>

    <div class="mt-4 text-center">
        <button class="btn btn-secondary" onclick="document.getElementById('details-container').innerHTML = '';">
            Fermer
        </button>
    </div>
</div>

<script>
    document.getElementById("details-container").classList.add("show");
</script>
{% endblock %}
