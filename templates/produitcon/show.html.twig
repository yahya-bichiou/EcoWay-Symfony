{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
    <h1>Produit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ produit.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ produit.description }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ produit.type }}</td>
            </tr>
            <tr>
                <th>Qualite</th>
                <td>{{ produit.qualite }}</td>
            </tr>
            <tr>
                <th>Quantite_disponible</th>
                <td>{{ produit.quantiteDisponible }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ produit.prix }}</td>
            </tr>
            <tr>
                <th>Date_ajout</th>
                <td>{{ produit.dateAjout ? produit.dateAjout|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_produitcon_index') }}">retour à la liste</a>

    <a href="{{ path('app_produitcon_edit', {'id': produit.id}) }}">modifier</a>

    {{ include('produitcon/_delete_form.html.twig') }}
{% endblock %}
