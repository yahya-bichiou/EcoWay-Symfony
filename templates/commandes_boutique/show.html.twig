{% extends 'base.html.twig' %}

{% block title %}CommandesBoutique{% endblock %}

{% block body %}
    <h1>CommandesBoutique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commandes_boutique.id }}</td>
            </tr>
            <tr>
                <th>OrderOwner</th>
                <td>{{ commandes_boutique.orderOwner }}</td>
            </tr>
            <tr>
                <th>OrderStatus</th>
                <td>{{ commandes_boutique.orderStatus }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_produits_boutique_index') }}">back to list</a>

    <a href="{{ path('app_commandes_boutique_edit', {'id': commandes_boutique.id}) }}">edit</a>

    {{ include('commandes_boutique/_delete_form.html.twig') }}
{% endblock %}
