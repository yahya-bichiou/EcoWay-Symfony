<form method="post" action="{{ path('app_commandes_boutique_delete', {'id': commandes_boutique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commandes_boutique.id) }}">
    <button class="btn">Delete</button>
</form>
