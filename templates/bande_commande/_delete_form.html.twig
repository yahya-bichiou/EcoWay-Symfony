<form method="post" action="{{ path('app_bande_commande_delete', {'id': bande_commande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bande_commande.id) }}">
    <button class="btn">Supprimer</button>
</form>
