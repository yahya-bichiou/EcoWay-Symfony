{% extends 'base.html.twig' %}

{% block title %}Modifier la Bande Réception{% endblock %}

{% block body %}

    <div class="container mt-4">
        <h1 class="text-center text-primary mb-4">Modifier la Bande Réception</h1>

        <div class="card shadow p-4">
            {{ include('bande_reception/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>

        <div class="mt-3 d-flex justify-content-between">
            <a href="{{ path('app_bande_reception_index') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Retour à la liste
            </a>
            {{ include('bande_reception/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
